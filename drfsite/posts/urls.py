# from django.urls import path
# from .views import ArticleView
# app_name = "posts"
# # app_name will help us do a reverse look-up latter.
# urlpatterns = [
#     path('posts/', ArticleView.as_view()),
#     path('articles/<int:pk>', ArticleView.as_view()),
r